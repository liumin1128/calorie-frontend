## 1. 存储上传与类型扩展

- [x] 1.1 新增前端对象存储上传服务，封装预签名请求、PUT 上传与公开 URL 返回
- [x] 1.2 扩展记录相关 TypeScript 类型与 DTO 构造，补齐 `images` 字段读写支持
- [x] 1.3 复用现有图片预处理能力，定义记录图片允许的文件格式、错误信息与公共上传入口

## 2. 图片附件状态层与 UI 拆分

- [x] 2.1 拆分 `ImageUploadAnalyzer` 的展示与逻辑，抽出可复用的图片附件状态管理能力
- [x] 2.2 为手动饮食记录编辑场景实现图片预览、替换、清空与已有远端 URL 回显
- [x] 2.3 为 AI 识图与条码扫码场景接入统一的图片附件状态与保存前上传能力

## 3. 记录保存链路改造

- [x] 3.1 改造 `CreateRecordDialog` 提交流程，在饮食记录保存前上传图片并把 URL 写入 `images`
- [x] 3.2 改造 `AiAnalysisPreview` 批量保存流程，确保同一张来源图片只上传一次并复用到每条记录
- [x] 3.3 改造 `useCalorieTracker` 与扫码确认流程，保存条码记录时携带来源图片 URL

## 4. 图片回显与验收

- [x] 4.1 在记录列表中为存在图片的记录增加缩略图展示，保持无图记录布局兼容
- [x] 4.2 在编辑回显中验证已有图片、替换图片、移除图片三种状态的更新行为
- [x] 4.3 补充必要的错误处理与手工验证，覆盖手动录入、AI 识图、扫码三条链路的图片保存与回显