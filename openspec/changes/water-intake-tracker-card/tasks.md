## 1. 类型与服务层

- [x] 1.1 新增饮水领域类型，定义查询结果项、查询响应和设置载荷
- [x] 1.2 新增 `waterService`，封装 `GET /water` 与 `PUT /water`，并提供按今日日期读取与覆盖设置的方法

## 2. 状态管理与逻辑封装

- [x] 2.1 新建 `waterIntakeStore`，集中管理今日饮水量、loading、submitting、error 状态
- [x] 2.2 在 store 中实现今日饮水初始化读取、按杯设置总量、失败回滚与禁用重复提交逻辑
- [x] 2.3 拆分饮水模块的容器层与纯视图层，确保展示组件不直接调用 API

## 3. UI 组件与首页集成

- [x] 3.1 新建独立饮水卡片，展示今日饮水总量、杯数摘要和 500ml 杯子计数器
- [x] 3.2 实现杯子点击交互，支持通过点击任意杯子直接设置累计饮水量
- [x] 3.3 将饮水卡片接入首页右栏，保持与个人摘要、日历卡片一致的视觉层级和响应式布局

## 4. 验证

- [x] 4.1 验证首页饮水卡片能正确读取今日饮水并完成更新
- [x] 4.2 运行受影响范围的类型检查或 lint，确认新增模块与首页集成无错误