## 1. 类型与服务层

- [x] 1.1 在 `src/types/calorie.ts` 中新增条码营养预览相关类型，定义前端统一消费的最小预览模型
- [x] 1.2 创建 `src/services/barcodeNutritionService.ts`，封装条码营养查询 API 调用、错误处理与响应适配

## 2. 条码营养预览界面

- [x] 2.1 创建条码营养预览组件，展示商品名称、条码号、热量和主要营养成分，并提供确认与取消操作
- [x] 2.2 在预览组件中补齐加载、空结果和错误提示状态，确保用户可恢复操作

## 3. 首页扫码流程集成

- [x] 3.1 调整 `BarcodeScanner` 的成功回调，让扫码组件输出识别结果而不是仅弹出提示
- [x] 3.2 在 `useCalorieTracker` 和首页组合层中接入“扫码成功 -> 查询营养 -> 打开预览”的状态编排
- [x] 3.3 实现预览确认后创建 `source="barcode"`、`externalId=<barcode>` 的饮食记录，并在成功后刷新首页数据

## 4. 验证

- [x] 4.1 验证扫码成功、查询成功并确认添加时，首页记录列表与统计会更新
- [x] 4.2 验证条码无匹配、接口失败、用户取消三类分支都不会误创建记录，且允许重新扫码